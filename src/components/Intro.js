import React from "react";
import Logo from "../assets/logo.png";

const Intro = () => {
  return (
    <div className="intro">
      <img src={Logo} alt="Logo" />
    </div>
  );
};

export default Intro;
